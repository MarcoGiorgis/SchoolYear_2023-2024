import turtle #esempio di importazione di un modulo bulit-in

finestra = turtle.Screen() #creazione finestra
alice = turtle.Turtle() #creazione alice
alice.forward(100) #allunga alice di 100 pixel
alice.left(30)
alice.forward(50)

finestra.mainloop() #mantine la finestra aperta